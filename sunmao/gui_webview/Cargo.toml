[package]
name = "sunmao_gui_webview"
version = "0.1.0"
edition = "2021"
description = "WebView renderer backend for SunMao GUI"
license = "MIT"

[dependencies]
sunmao_gui = { path = "../gui", default-features = false, features = ["webview"] }
