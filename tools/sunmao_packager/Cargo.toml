[package]
name = "sunmao_packager"
version = "0.1.0"
edition = "2021"
authors = ["aizcutei"]
description = "Unified packager for AudioUnit, VST3, and CLAP plugins"
license = "MIT OR Apache-2.0"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
anyhow = "1.0"
