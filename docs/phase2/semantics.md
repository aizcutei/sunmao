# Phase 2 VST3 ↔ CLAP 语义映射

每个 Phase 2 host-facing 能力在两种格式落地时，本表记录语义差异与降级行为。
"降级"指一种格式缺少对应概念时 SunMao 统一 API 的行为；严禁静默丢事件。

| 能力 | SunMao API | VST3 | CLAP | 差异与降级 |
|---|---|---|---|---|
| transport/timing | `ProcessContext` 的 `tempo`/`is_playing`/`is_recording`/`is_loop_active`/`sample_pos`/`time_signature`/`song_pos_beats`/`song_pos_seconds`/`bar_start_beats`/`bar_number`/`loop_beats`（M1 落地） | `ProcessData.processContext`（state flags 标记各字段有效性） | `clap_process.transport`（`clap_event_transport`，flags 同样按位标记） | 两侧字段均可缺失，统一 API 一律 `Option`，`None` 表示"宿主未提供"而非 0。单位已归一：CLAP beattime 定点（`CLAP_BEATTIME_FACTOR`）与 VST3 `TQuarterNotes` 都换算为**四分音符** `f64`。宿主完全不给 transport（CLAP `transport` 为 NULL、VST3 `process_context` 为 NULL）时全部音乐字段为 `None` 且 `is_playing` 保持 true。测试：`clap_transport_fields_reach_the_plugin`、`vst3_transport_fields_reach_the_plugin`、两侧 `a_host_without_a_*_leaves_every_musical_field_absent` |
| bar 序号 | `ProcessContext::bar_number` | 无对应字段（只有 `bar_position_music`，小节的音乐起点） | `clap_event_transport.bar_number` | **VST3 降级为 `None`**；`bar_start_beats` 两侧都可用，需要序号的插件应改用它或自行累计。测试：`vst3_transport_fields_reach_the_plugin` 显式断言 `bar_number == None` |
| loop 区间 | `ProcessContext::loop_beats`（四分音符） | `cycle_start_music`/`cycle_end_music` + `kCycleValid`/`kCycleActive` | `loop_start_beats`/`loop_end_beats` + `CLAP_TRANSPORT_IS_LOOP_ACTIVE` | 两侧一致：loop 未激活或区间非法（`end <= start`、非有限）时为 `None`，绝不下发负长度区间。CLAP 另有秒制 loop 字段，统一 API 只承诺 beats（两格式均原生提供），秒制保留在 `clap_rs::Transport::loop_seconds`。测试：`loop_region_requires_an_active_loop_and_a_sane_range`、`cycle_region_requires_an_active_cycle_and_a_sane_range` |
| 秒制位置 | `ProcessContext::song_pos_seconds` | 无秒字段，由 `project_time_samples / sample_rate` 推导 | `song_pos_seconds`（定点） | VST3 侧为推导值而非宿主原值；`sample_rate <= 0` 时 `None` |
| latency | `SunmaoPlugin::latency_samples()`（M2 落地） | `IAudioProcessor::getLatencySamples`，变更需 `IComponentHandler::restartComponent(kLatencyChanged)` | `clap.latency` 扩展 + `host_latency.changed()`（只能在重启/去激活窗口生效） | 变更通知时机不同，统一 API 因此约定"**仅在非 processing 状态可变**"：`initialize` 或 `set_render_mode` 里改，processing 期间必须恒定。**Phase 3 M1 修正的实现缺陷**：CLAP backend 的 `activate` 会把插件 `take()` 进 audio processor，原实现的 `latency()`/`tail()` 只看 `self.plugin.as_ref()` 并 `unwrap_or(0)`，因此**在插件激活期间（正是宿主真正查询的时刻）一律上报 0**——宿主会据此不做任何延迟补偿、并可能切掉尾音；VST3 backend 直接持有插件故无此问题，两格式行为分叉。现改为在 `activate` 移交所有权前缓存（此时 `initialize` 已跑完，值反映激活采样率），激活期间回落到缓存值。该缺陷由 Phase 3 M1 第 3 项新增的 runner 宿主侧断言发现——既有单测只覆盖了未激活状态。测试：`latency_and_infinite_tail_reach_the_vst3_contract`（走真实 `IAudioProcessor` vtable）、`latency_and_infinite_tail_reach_the_clap_contract`、`latency_and_tail_survive_activation`（激活期间仍可读）、runner 的 `latency_tail`（对 Tempo Delay 断言非零：两格式均读到 221 samples ＝ 44.1kHz 下 5ms lookahead，且各自使用本格式的无限尾音魔数 `i32::MAX`/`u32::MAX`）。**Phase 3 M4 追加**：上报的数值必须等于真实群延迟，而不只是非零——runner 新增 `latency_alignment`，对 `SunMao OS Distortion`（`sunmao/dsp` 4x oversampler，线性相位半带 FIR，`OversamplingFactor::latency_samples()` = 24）经两格式 API 读取 latency 后送单位冲激、定位输出峰值帧，要求 |峰值帧 − 上报值| ≤ 1；该 fixture 从 `OversamplingFactor` 而非已 prepare 的 oversampler 回答 latency，因此在 VST3 `setActive` 之前查询也得到同一数值（`latency_is_reported_before_the_plugin_is_initialized`）。Tempo Delay 不做此断言：其冲激峰值在延迟时间处而非 lookahead 处，线性相位 lookahead 才有“峰值＝latency”的性质 |
| tail | `SunmaoPlugin::tail() -> TailLength{None,Samples(u32),Infinite}`（M2 落地） | `getTailSamples`：`kNoTail`=0，`kInfiniteTail`=`u32::MAX` | `clap.tail`：≥`i32::MAX` 即视为无限 | 两格式的"无限"是不同魔数，统一 API 用枚举表达，backend 负责编码。**有限 tail 必须被夹到魔数之下**（VST3 夹到 `kInfiniteTail-1`，CLAP 夹到 `i32::MAX-1`），否则一个恰好等于魔数的有限值会被宿主静默当成无限尾音。测试：`a_finite_tail_never_collides_with_the_infinite_encoding`、`a_finite_clap_tail_never_reaches_the_infinite_threshold` |
| offline render | `SunmaoPlugin::set_render_mode(RenderMode{Realtime,Offline})`（M2 落地） | `ProcessSetup.process_mode`（`kRealtime`/`kPrefetch`/`kOffline`），由 `setupProcessing` 下发 | `clap.render` 扩展的 `set()` | VST3 的 `kPrefetch` 与任何未知模式都映射为 `Realtime`（较严格的契约）；两侧都只在非 processing 状态下发，因此插件可借此改 latency。CLAP 侧 `set_render_mode` 经 `catch_unwind` 包裹，插件 panic 转为 `false` 而非跨 ABI 展开。测试：`vst3_rs::RenderMode::from_process_mode`、fixture 的 `offline_rendering_doubles_the_reported_lookahead` |
| 多 bus/sidechain | `SunmaoPlugin::input_buses()`/`output_buses() -> Vec<BusInfo>`，`BusInfo::{main,sidechain}`，`BusRole::{Main,Sidechain}`；处理侧 `AudioBuffer::{num_input_buses,input_bus_channels,input_bus}`（M3 落地） | `getBusCount/getBusInfo/setBusArrangements`；`BusRole::Sidechain → PortType::Aux → BusTypes::kAux`；speaker layout 只应用于 main bus，aux 由通道数推断 | `clap.audio-ports`：无 aux 概念，`BusRole::Main → is_main=true`，sidechain 即 `is_main=false` 的普通端口 | 声明的 bus 是**通道拓扑的唯一真相**：两个 backend 的扁平通道总数都取自 bus 声明之和，因此插件加 sidechain 只需覆写 `input_buses()`。默认实现由 `input_channels()`/`output_channels()` 推导单 main bus，Phase 1 插件行为不变。宿主未连接某 bus 时 `input_bus_channels()` 返回 `None`（而非 panic 或读到别的 bus 的数据），插件可据此回落。测试：`input_bus_bounds_split_the_flat_channel_list`、`an_absent_bus_reads_as_empty_rather_than_panicking`、fixture 的 `the_plugin_declares_a_stereo_sidechain_bus`/`a_loud_key_ducks_a_quiet_main_signal`/`a_silent_key_leaves_a_loud_main_signal_alone` |
| bus 激活/去激活 | `SunmaoPlugin::set_bus_active(is_input, bus_index, active) -> bool`（Phase 3 M1 落地） | `IComponent::activateBus`，按 `MediaType`/`BusDirection`/index 定位 | `clap.audio-ports-activation/2` 的 `set_active(is_input, port_index, is_active, sample_size)`；`can_activate_while_processing` 固定 `false` | `bus_index` 即 `input_buses()`/`output_buses()` 的声明序号，两个 backend 都**先按声明校验索引**，越界一律拒绝（VST3 `kInvalidArgument`、CLAP `false`），插件回调只会见到真实存在的 bus。插件返回 `false` 时如实上报（VST3 `kResultFalse`、CLAP `false`），不静默接受。差异：**CLAP 多一个 `sample_size` 参数**（宿主的采样位宽），SunMao 只支持 f32 且 clap_rs 在 activate 阶段已拒绝其它位宽，故该参数在 backend 层被丢弃、不进统一 API；**VST3 的 event bus** 由 wrapper 自行接受（无插件侧状态可切），不进回调。两侧都只在插件未激活时调用，因此回调内可重配处理状态。去激活的输入 bus 仍占据 `AudioBuffer` 的通道槽位，插件应据此跳过读取而非期待 bus 消失。测试：`clap_rs::audio_ports_activation_validates_the_index_and_forwards_to_the_plugin`、`clap_rs::plugins_without_audio_ports_do_not_expose_the_activation_extension`、`vst3_rs::activate_bus_validates_the_index_and_forwards_to_the_plugin`、`vst3_rs::activate_bus_before_initialize_reports_not_initialized`、backend 的 `clap_port_activation_maps_onto_the_sunmao_bus_callback`/`clap_declares_one_audio_port_per_sunmao_bus`/`vst3_activate_bus_maps_onto_the_sunmao_bus_callback`、fixture 的 `deactivating_the_key_bus_returns_the_detector_to_the_main_path`/`activating_the_main_buses_is_always_accepted`；索引校验不变量由 proptest 钉住：`clap_rs::port_activation_forwards_exactly_the_declared_indices`、`vst3_rs::activate_bus_forwards_exactly_the_declared_indices`（任意声明拓扑 × 任意索引，含负数与越界：在范围内必转发且仅转发一次，越界必拒绝且绝不触达插件） |
| speaker layout 动态协商 | `SunmaoPlugin::bus_configs() -> Vec<BusConfig>` + `current_bus_config()` + `select_bus_config(index) -> bool`（Phase 3 M1 落地） | `IAudioProcessor::setBusArrangements`：宿主**提议**每个 bus 的 `SpeakerArrangement`，backend 按提议的通道数（位图 popcount）在 `bus_configs()` 中查找匹配项；命中即 `select_bus_config` 并 `kResultOk`，未命中 `kResultFalse` | `clap.audio-ports-config`：插件**枚举**配置、宿主按 id `select`；另实现 `clap.audio-ports-config-info/1`（含 draft-0 别名）供宿主查询未激活配置的端口 | **两格式方向相反但落到同一回调**：CLAP 是"插件列举 → 宿主选"，VST3 是"宿主提议 → 插件匹配"，因此 **VST3 宿主只能到达 `bus_configs()` 已发布的布局**——未发布的提议一律 `kResultFalse`，不静默接受后跑另一种布局。config id 即 `bus_configs()` 的下标，两侧同一编号。`bus_configs()` 为空（默认）＝不可协商，此时 CLAP **不暴露**该扩展、VST3 只接受声明布局，Phase 1/2 插件行为不变。协商成功后两侧都刷新派生状态：VST3 的 `getBusInfo`/`getBusArrangement`/`setupProcessing` 改报新通道数，CLAP 重建端口表、通道总数与 audio-thread scratch buffer（否则 mono→stereo 会用旧尺寸缓冲处理）。**限制**：只协商通道数，bus 数量不变；VST3 仅在组件未 active 时接受协商（`kResultFalse`），CLAP 由规范保证 deactivated。**顺带修正**：`clap_rs` 此前对所有端口固定上报 `port_type=stereo`，mono 布局出现后即为错报，现按通道数给 `mono`/`stereo`/null。一期只承诺 mono/stereo，surround 位图预留。测试：`clap_rs` 侧 `clap_publishes_one_config_per_sunmao_bus_config`/`clap_selecting_a_config_reconfigures_the_live_port_list`/`clap_refuses_a_config_the_plugin_rejects`/`a_plugin_without_alternatives_publishes_no_configs`、`vst3_negotiates_a_published_layout_and_reports_it_back`、`vst3_refuses_every_alternative_for_a_single_layout_plugin`、fixture `sunmao_fx_layout_gain` 的 5 个测试、跨格式可达性 proptest `a_layout_is_reachable_from_both_formats_or_neither` |
| state 版本与迁移 | `SunmaoPlugin::STATE_VERSION` + `migrate_state(from_version)`（M5 落地，Phase 3 M1 完成 backend 接线） | `IComponent::setState`/`getState` 经 `IBStream`，blob 头 `SMV3PRM\0` + version + count | `clap.state` 的 `load`/`save` 经 `clap_istream_t`/`clap_ostream_t`，blob 头 `SMCLPRM\0` + version + count | 两格式各自的 blob 头布局相同、magic 不同（互不通用，符合各格式宿主只读自己格式的现实）。**Phase 3 M1 修正的实现缺陷**：两个 `_rs` 层此前把版本号**硬编码为 1** 写入，插件声明的 `STATE_VERSION` 从未被写入或比对，因此 `migrate_state` 在任何情况下都不会被调用——接线缺口不在 core（钩子早已就绪）而在 `_rs`+backend。现改为写入插件自己的版本，载入时按插件版本比对：**更旧接受并回调 `migrate_state(from_version)`、同版本不回调、更新拒绝**。迁移只在全部参数值应用完毕后触发，插件因此总是从一个完整的旧状态迁移，而非半应用状态。**一次性兼容影响**：由修复前的构建写出的 blob 一律被标为 1，即便插件当时已是 v2；修复后这些 blob 会被当作 v1 读入并触发 `migrate_state(1)`。`sunmao_state_migration` fixture 的迁移是幂等的（把 trim 设为常量），故无害；任何非幂等迁移的插件需自行考虑该一次性影响。测试：`clap_state_from_an_older_build_triggers_migration`、`clap_saved_state_carries_the_plugin_state_version`、`vst3_state_from_an_older_build_triggers_migration`（含写出版本断言），三者都走各自格式的真实 stream ABI；fixture 的 `sunmao_state_migration` 保持 round-trip 绿 |
| preset 载入 | `SunmaoPlugin::SUPPORTS_PRESET_LOAD` + `load_preset(PresetLocation) -> bool`，`PresetLocation::{File{path,key}, Internal{key}}`（Phase 3 M1 落地） | **无对应调用**：`vst3_sys` 未绑定 `IUnitInfo`/`IProgramListData`，VST3 宿主载入 preset 的实际路径是把 preset 文件字节交给 `setState`——即走"状态应用"那一半，`load_preset` 永不被调用 | `clap.preset-load/2`（含 `clap.preset-load.draft/2` 别名）的 `from_location(location_kind, location, load_key)` | 统一 API 是"**插件侧载入回调 + 状态应用**"两条腿：CLAP 宿主只报位置、由插件自己读取（回调腿），VST3 宿主直接送字节（状态腿，已由 state 契约覆盖）。**这是本表少见的单格式能力**，按边界"program list 可选实现"处理：VST3 program list 未实现，且**不存在静默丢事件**——VST3 宿主根本没有可调用的 preset 接口，不是调用了被丢弃。扩展仅在 `SUPPORTS_PRESET_LOAD` 为真时暴露，否则宿主不会拿到一个只会失败的 loader。backend 层的防御：`location` 为 file 却是空指针、非 UTF-8 路径、未知 location_kind 一律在触达插件前拒绝（**不做有损转换**，否则会载入与宿主所指不同的文件）；插件返回 `false` 如实上报，宿主据此报错而非误以为成功。测试：`clap_preset_load_reaches_the_plugin_with_its_location`（走真实扩展，断言 file/internal 两种位置原样送达、插件拒绝上报 false、空路径与未知 kind 不触达插件）、`a_plugin_without_preset_support_does_not_expose_the_extension`、fixture 的 `a_named_factory_preset_is_applied`/`an_unknown_or_file_preset_is_refused_rather_than_faked` |
| 参数分组/嵌套 | `ParamDescriptor.group`（`/` 分隔路径，空＝顶层）+ derive 的 `#[group = "Osc/Tuning"]`；辅助函数 `params::group_segments`（Phase 3 M2 落地） | `IUnitInfo`（**本轮新增 `vst3_sys` 绑定**，IID 自上游 `vst/ivstunits.h` 转录）：路径经 `vst3_rs::units::UnitTable` 展开为 unit 树，每个 unit 有 id/parent_id/**单层名**，参数经 `ParameterInfo.unit_id` 指向所属 unit | `clap_param_info_t.module`：直接就是 `/` 分隔路径字符串 | **两格式表达方式不同**：CLAP 每参数一个路径字符串，VST3 要一棵 unit 树 + 参数指 id，故统一 API 用路径、由 backend 各自编码。中间层级即使无参数直接命名也会被创建（`"Osc/Tuning"` 单独出现时仍生成 `Osc`），否则宿主会拿到 parent 不存在的 unit。unit 列表保证**父先于子**（proptest 式断言 `every_parent_is_declared_before_its_children`）。空路径/多余斜杠被规范化（丢弃空段），不会产生无名层级。未声明分组的插件**不暴露 `IUnitInfo`**（`kNoInterface`），避免给宿主一棵空树。**顺带修正**：`clap_rs` 此前把 `info.module` 无条件清零，声明的层级根本到不了宿主。**未做**：program list（`IUnitInfo` 的 program 相关方法一律 `kNotImplemented`，与 preset 载入行的边界一致）。测试：`vst3_rs::units` 6 个（含父子顺序、中间层级、斜杠规范化）、`clap_reports_each_parameter_group_as_a_module_path`（走真实 `clap.params`，断言路径逐字送达）、`vst3_exposes_the_group_tree_through_iunitinfo`（走真实 `IUnitInfo`+`IEditController`，断言 unit 树、单层名、越界索引被拒、每个参数的 `unit_id` 指向正确 unit、无分组参数留在 root）、`a_flat_plugin_does_not_expose_iunitinfo`、fixture 的 `every_parameter_declares_its_host_visible_group`/`the_declared_groups_form_one_coherent_tree` |
| 参数 modulation | `Event::ParamMod{id,amount,offset}` + `EventQueue::param_mods()`（M4 落地） | 无原生概念，VST3 宿主不会发送，故队列中不会出现该事件 | `CLAP_EVENT_PARAM_MOD` → `clap_rs::Event::ParamMod` → core | **modulation 与 automation 是两条独立通道**：`Event::as_param_change()` 对 `ParamMod` 返回 `None`，`param_changes()` 不含 modulation，因此 mod 绝不会流入插件 state。amount 是叠加偏移而非归一化值，只拒绝非有限值。测试：`param_mod_events_are_decoded_rather_than_dropped`、`short_expression_and_mod_events_are_rejected` |
| per-note expression | `Event::NoteExpression(NoteExpression{offset,kind,note_id,channel,key,value})` + `EventQueue::note_expressions()`；`NoteExpressionKind{Volume,Pan,Tuning,Vibrato,Expression,Brightness,Pressure,Unknown(i32)}`（M4 落地） | `kNoteExpressionValueEvent` 只携带 `type_id`/`note_id`/`value`；type id 见新增的 `vst3_sys::NoteExpressionTypeIDs` | `CLAP_EVENT_NOTE_EXPRESSION`，携带 note_id + port/channel/key | **VST3 只按 note_id 定位音符**，因此统一结构的 `channel`/`key` 是 `Option`：VST3 侧为 `None`，CLAP 侧为 `Some`。插件应优先按 note_id 匹配，退化时按 channel/key 匹配（fixture 即如此）。**VST3 没有 pressure 维度**（宿主用独立的 `kPolyPressureEvent` 传递），故 VST3 侧永不产生 `Pressure`。未知维度保留原始 id 为 `Unknown(i32)` 并照常下发，绝不丢事件。**Phase 3 M1 修正的实现缺陷（VST3 侧 expression 此前根本到不了插件）**：VST3 宿主经回调下发 expression，而 backend 原实现在回调里直接 push 进 `self.event_queue`；但 `process` 会在合并本块事件前 `self.event_queue.clear()`，**该 clear 发生在回调之后**，于是每个 VST3 note expression 都被静默丢弃——违反本文件“严禁静默丢事件”的约定。MIDI 未受影响是因为它走 `pending_midi` 暂存再合并，expression 没有对应暂存。现新增 `pending_expressions`（与 `pending_midi` 同容量策略、同 `try_reserve_exact` 预分配，故 audio callback 仍零分配），并把 param/MIDI/expression 三路按 sample offset 归并（并列时序为 param → MIDI → expression，确定性）。此缺陷 `_rs` 层与 core/fixture 两端的既有测试都测不到（vst3_rs 正确分发、core 正确处理，缺口恰在中间的 backend），正是 Phase 3 M1 第 4 项“backend 层端到端映射测试”要覆盖的位置。测试：`note_expression_events_are_decoded_rather_than_dropped`、`an_unknown_expression_dimension_keeps_its_raw_id`、backend 端到端 `raw_vst3_expression_events_reach_the_core_queue`（构造原始 `NoteExpressionValueEvent` 走真实 `IEventList`，断言 kind/note_id/value/offset、VST3 侧 channel/key 为 `None`、未知 id 保留原值，并断言与 MIDI 按 offset 交错）、`raw_clap_expression_and_mod_events_reach_the_core_queue`（原始 `clap_event_note_expression_t` 与 `clap_event_param_mod_t` 走真实 `clap_input_events_t`，断言 CLAP 侧 channel/key 为 `Some`、param mod 按数值 id 译回字符串 id、且 modulation 不出现在 automation 流）、fixture 的 `a_volume_expression_scales_only_the_addressed_voice`/`a_tuning_expression_bends_the_addressed_voice`/`an_expression_for_another_key_leaves_the_voice_untouched`/`an_unnamed_expression_dimension_is_accepted_without_effect` |
| voice-info | `SunmaoPlugin::voice_info() -> Option<VoiceInfo{active,capacity,supports_overlapping_notes}>`（M4 落地） | 无对应接口 | `clap.voice-info` 扩展 | **VST3 降级：该信息不向宿主暴露**（VST3 无对应查询点），插件实现同一方法即可，不需要格式分支。默认返回 `None`，表示"非发声类插件"。 |
| 版本化 state（Phase 2 M5 的原始条目；**当前行为见上方"state 版本与迁移"行**，Phase 3 M1 修正了两个 `_rs` 层把版本硬编码为 1 的缺陷） | `SunmaoPlugin::STATE_VERSION` 常量 + `migrate_state(from_version)` 钩子（M5 落地） | `IComponent::setState/getState` 字节流，编解码在 `vst3_rs/src/state.rs` | `clap.state` 字节流，编解码在 `clap_rs/src/ext/state.rs` | 两侧都是不透明字节流，头部结构（magic + version + count + 按参数 id 的条目）在两个格式层保持一致。**版本策略**：旧版本 state 被接受（条目按参数 id 匹配，老参数恢复、新参数留默认值），未来版本被拒绝（本 build 无法解释）。此前两侧都对版本不符直接拒绝，旧 preset 会整体丢失——这是 M5 修复的本体。增删参数不需要升版本，只有**既有参数的含义变化**才需要。测试：两侧各 `a_state_from_an_older_build_is_accepted`/`a_state_from_a_newer_build_is_rejected`/`a_foreign_magic_is_rejected`，fixture 的 `a_v1_state_leaves_the_new_parameter_at_its_documented_default` |
| preset 载入（Phase 2 M5 的设计条目） | 已落地，见上方"preset 载入"行 | 同上 | 同上 | 本行是 Phase 2 规划期写下的占位。按本文件的记录规则，"设计中"必须在落地时替换——该能力已于 Phase 3 M1 第 6 项落地（`SUPPORTS_PRESET_LOAD` + `load_preset`），最终 API、单格式降级与测试名都在上方那一行。保留本行只为不让旧链接失效 |
| 乐器声明 | `SunmaoPlugin::IS_INSTRUMENT`（Phase 3 M5 落地，带默认值 `false` 的关联常量） | 由 `input_channels()`/`accepts_midi()` 的默认实现推导，落到既有的 `getBusCount`/`getBusInfo`（无 audio input bus）与 event input bus | 同样经既有路径：`clap.audio-ports`（无输入端口）与 `clap.note-ports`（一个 MIDI 输入端口） | **不是新的宿主能力，而是防止把既有的两个开关只设对一半**：宿主判定"乐器"要看"无音频输入"**和**"接收 MIDI"两处，分开声明时漏掉 `accepts_midi` 的乐器会以静音效果器的身份加载。声明一次由默认实现推导两处，两个方法仍可各自 override（带 sidechain 的乐器、要 MIDI 的效果器）。两格式行为一致，无降级。测试：`declaring_an_instrument_sets_both_switches_a_host_reads`、`either_switch_can_still_be_overridden`、`the_default_input_bus_follows_the_instrument_declaration`；宿主侧由 `sunmao_template_instrument` 在打包矩阵里经两格式跑 runner 的 synth 套件验证（`synth_layout_and_idle` 断言 0 in、`synth_note_on` 断言按 sample offset 起音） |
| GUI DPI scale 协商 | `ViewHandle::scalable(value, resize, set_scale)` 构造 + `ViewHandle::set_scale(f32) -> bool`（Phase 4 M1 落地） | `IPlugViewContentScaleSupport::setContentScaleFactor(ScaleFactor)`，**本轮新增 `vst3_sys` 绑定**（IID `0x65ED9690,0x8AC44525,0x8AADEF7A,0x72EA703F` 与 `typedef float ScaleFactor` 均自上游 `gui/iplugviewcontentscalesupport.h` 转录）；宿主经 `IPlugView::queryInterface` 索取该可选接口 | `clap_plugin_gui.set_scale(plugin, scale: f64) -> bool` | **精度不同**：VST3 的 `ScaleFactor` 是 `f32`，CLAP 是 `f64`；统一 API 取 `f32`（scale 是 1.0/1.5/2.0 这类小比值，多出的精度无显示意义），CLAP backend 在收窄前**拒绝**超出 `f32::MAX` 的值而不是让它变成 infinity。**拒绝的信号方式不同**：非有限/非正因子在 VST3 侧回 `kInvalidArgument`（一个错误 tresult），CLAP 侧回 `false`；runner 两种都接受为“已拒绝”。**“不支持”与“错误”必须分开**：编辑器自己处理缩放（macOS 由 AppKit 拥有 backing scale，宿主通常根本不调）时返回 `false` → VST3 回 `kResultFalse` 而非错误码，这是正常答复不是失败。校验只做一次：`ViewHandle::set_scale` 统一挡掉非有限与非正值，两个 wrapper 不重复实现，插件代码永远见不到非法因子。**本轮修正的既有缺陷**：`clap_rs` 早已导出 `set_scale` 入口，但 `sunmao/backend_clap` 从未 override `GuiHandler::gui_set_scale`，因此一直用默认实现回 `false`——CLAP 宿主被告知“不支持缩放”；VST3 侧则连 `_sys` 绑定都没有。core 里 `SunmaoView::set_scale_factor` 这个钩子自 Phase 1 起就存在却**从无调用方**，且签名是 `&self` 根本无法改动编辑器，本轮已删除，改由持有 `&mut` 的 `ViewHandle` 承载。baseview 适配层把因子实现为“重设窗口为 `创建尺寸 × factor`”，即非 DPI-aware 编辑器在 Windows/X11 上应有的反应。测试：`vst3_rs::plug_view_reports_content_scale_support_and_forwards_the_factor`（走真实 vtable：queryInterface 拿到接口、因子送达、`kResultFalse`/`kInvalidArgument` 分支、refcount 与视图共享）、`vst3_rs::plug_view_scale_vtbl_is_one_pointer_after_vtbl`（钉住字段偏移算术）、`sunmao_core::view_handle_validates_scale_before_reaching_the_editor`、`sunmao_backend_clap::clap_scale_narrows_f64_and_reports_no_editor_as_unsupported`；宿主侧由 runner 的 `gui-test` 在两格式上实测（应用 2.0、拒绝 0.0、恢复 1.0），随 GUI 矩阵在三平台运行 |
| GUI 键盘转发 | `SunmaoView` 侧经 `ViewHandle::send_key(ViewKey)`；编辑器内 `Stack` 焦点模型 + 控件键盘处理（Phase 4 M3 落地） | `IPlugView::onKeyDown`/`onKeyUp(char16, int16 keyCode, int16 modifiers)`。**此前两者都是返回 `kResultFalse` 的 stub**，宿主转发的按键从来没到过插件。keyCode 是 SDK 自己的 `KeyCodes` 枚举（**不是**平台 scancode、**不是** ASCII），已把该枚举转录进 `vst3_sys::base::keycodes` | **无对应接口**：CLAP 编辑器自己的窗口直接从操作系统收键盘，宿主没有可转发的入口 | **这是本表第二条单格式能力**，与 preset 载入同类处理：**不存在静默丢事件**——CLAP 宿主根本没有可调用的转发接口，不是调用了被丢弃。统一 API 用**格式中立**的 `ViewKeyCode` 而非原始码：VST3 的编号只有 backend 认识，让它一路传到 GUI 层会使每个消费者都依赖某一格式的编号。**本轮真实教训**：该编号最初是凭记忆写的，`KEY_RETURN`/`KEY_LEFT`/`KEY_UP` 等 13 个值里 12 个是错的，而单测**照着错误实现写因此全绿**——按上游 `keycodes.h` 核对后才发现。现有 `vst3_key_codes_match_the_upstream_numbering` 直接钉住上游整数字面量而不是引用常量，使常量本身写错也会被抓到。编辑器不要的键回 `kResultFalse`，宿主因此保留自己的快捷键——全部吞掉会让 DAW 在插件窗口获得焦点时键盘失灵。**限制**：目前只有 GL 后端从队列取键（wgpu/WebView 编辑器如实回“未使用”），因为宿主在它自己的线程调用而控件活在窗口线程，跨线程要靠每帧排空的有界队列（上限 64）。测试：`vst3_rs::plug_view_forwards_host_keys_to_the_plugin`（走真实 vtable，含 press/release、字符解码、`kResultFalse` 分支）、`sunmao_backend_vst3::vst3_key_codes_match_the_upstream_numbering`、`sunmao_view_baseview::every_neutral_key_maps_to_a_framework_code`/`the_host_key_queue_drains_in_order_and_refuses_to_grow_without_bound`、`sunmao_gui` 的焦点与控件键盘测试；宿主侧由 runner `gui-test` 实测（Tab 聚焦 → End 推到最大 → **经宿主 API 读回参数确认真的变了**，本地实测 `Gain moved 0 -> 1`，CLAP 侧走 skip 路径） |
| GUI 文本输入（国际键盘/IME） | `Event::TextInput { text }`（Phase 4 M3） | 随 `onKeyDown` 的 `char16` 转发；编辑器也接收原生窗口事件 | 编辑器直接接收原生窗口事件 | `Key::Character` 转文本、过滤 composing 与控制字符的适配器契约已有单测，但这些合成事件**不能证明平台输入法已经处理文本**。X11 本轮以服务器 XKB 布局替换硬编码 US，复用 locale compose，监听布局变化并在失焦时清理组合序列；新增真实 XTest 按键验收 `native_x11_layout_changes_and_compose_reach_the_window`（`90bf6de` / [run 34255696871](https://github.com/aizcutei/sunmao/actions/runs/34255696871) 三平台完整 jobs 成功、三份产物 SHA-256/ZIP CRC 校验通过，Linux 原始日志确认 X11 KEYBOARD VERIFIED 与 1 passed / 0 failed / 0 ignored）。这条路径是国际键盘/compose，不是 XIM 预编辑协议。macOS 现以每窗口 UCKeyTranslate/系统 Unicode 布局数据处理 dead-key compose，失焦、编辑/导航取消清理状态，按键释放与重复保留按下时逻辑字符；原生 `macos_keyboard` 主线程 NSView harness 自动选择 US/ABC 后恢复原输入源，验证 Option+å、组合 é 和 focus reset；`e14e5b3` / [run 34361057036](https://github.com/aizcutei/sunmao/actions/runs/34361057036) 三平台完整 jobs success，macOS 原始日志第 3107 行实际输出 MACOS KEYBOARD VERIFIED，第 3159 行按键生命周期属性测试通过。三平台产物均已完整下载，SHA-256 与 ZIP CRC 全部通过，macOS 国际键盘/compose 路径正式验收完成。Windows 审计发现 WH_GETMESSAGE 钩子在宿主 TranslateMessage 前吞掉按键，导致系统组合字符无法生成；当前补为钩子先调用 TranslateMessage，再由现有适配器接收 WM_CHAR/WM_SYSCHAR，同时在窗口回调前释放注册表读锁。新增 windows_keyboard 原生消息队列 harness（德语 z/ü、Shift Ü、dead-key é），已由 `d533e54` / [run 34446209752](https://github.com/aizcutei/sunmao/actions/runs/34446209752) 三平台完整 jobs success 与三份产物 SHA-256/ZIP CRC 验收；Windows 完整原始日志第 2878 行实际输出 WINDOWS KEYBOARD VERIFIED。harness 在建窗/聚焦后激活德语布局，每次按键前后核实布局稳定，不直接提供 WM_CHAR/Unicode，由系统 TranslateMessage 生成字符。不宣称完整 CJK IME 支持。适配器测试：`international_text_reaches_the_editor_as_text_input`、`composition_in_progress_produces_no_text`、`control_characters_are_not_treated_as_text`、`a_character_press_dispatches_both_the_key_and_its_text`。 |
| floating editor（独立窗口） | `SunmaoView::supports_floating()` + `open_floating(context)`（Phase 4 M5 落地；`baseview::Window::open_floating` 三平台各一条实现） | **无对应概念**——VST3 编辑器一律嵌入宿主提供的父窗口，`IPlugView` 没有浮动模式。降级方式：VST3 侧永远走嵌入路径，**不存在静默丢事件**（宿主根本没有可调用的浮动接口） | `clap_plugin_gui` 的 `is_floating` 参数 + `set_transient` + `suggest_title` | **CLAP 独有，现已真实支持**。此前一律拒绝，理由是 vendored baseview 只有 `open_parented` 与**阻塞式** `open_blocking`；重读三平台实现后发现该判断过重：`open_blocking` 里真正阻塞的只有最后一步（macOS 的 `app.run()`、Windows 的 `GetMessageW` 泵、X11 的 join），**窗口创建本身早已与事件循环分离**。于是新增 `open_floating`：macOS 复用 `open_blocking` 的建窗路径但 **`ns_app: None`**（该字段意为"本窗口拥有 application"，非空时关窗会调 `stop_application_event_loop()`——在插件里那会停掉**宿主的** run loop），且不碰 `setActivationPolicy`/`finishLaunching`/`activateIgnoringOtherApps`，并用 `orderFront_` 而非 `makeKeyAndOrderFront_` 以免抢走宿主焦点；Windows 直接取 `Self::open(false, null_mut(), ..)` 已经返回的 handle（宿主的消息泵按 HWND 派发，自然驱动我们的窗口）；X11 走 `open_parented` 的结构但 parent 传 `None`（X11 连嵌入窗口都自带窗口线程，差别只在不 join）。**查询与创建严格一致**：`is_api_supported(_, is_floating=true)` 回的是 `view.supports_floating()`，与 `gui_create` 同源。窗口在 **`gui_show`** 才真正打开（CLAP 的 create/show 分工：create 分配、show 上屏），浮动模式下 `gui_set_parent` 一律回 false。**仍降级的一项**：`gui_hide` 回 false——baseview 没有"隐藏但保活"的操作，关掉再开会让宿主拿到一个全新编辑器、丢失界面状态，故如实回不支持、让宿主退回 destroy/create。`suggest_title` 已于本轮由静默 stub 改为真实转发（非 UTF-8 丢弃而不做有损转换）。测试：`sunmao_backend_clap::a_floating_capable_view_opens_on_show_and_refuses_a_parent`、`a_floating_capable_view_still_embeds`、`a_suggested_title_reaches_the_plugin_and_a_non_floating_view_declines` |
| Wayland 上的编辑器 | 无专门 API：`SunmaoView` 不区分 X11/Wayland（Phase 4 M5 落地；原生路径限 CLAP 浮动 + GL） | **VST3 没有 Wayland 平台类型**：嵌入编辑器继续使用 `X11EmbedWindowID`，Wayland 桌面通过 XWayland 运行 | 上游 `gui.h` 对 `CLAP_WINDOW_API_WAYLAND` 明确规定 "embed is currently not supported, use floating windows"；浮动模式允许 null API | **嵌入式 Wayland 如实拒绝**，X11 嵌入路径保留。启用 facade 的 `gui-wayland`（包含 `gui-gl`，传递到 sunmao_view_baseview/wayland 与 baseview/wayland；默认关闭）且存在 `WAYLAND_DISPLAY` 时，浮动编辑器使用原生 `wl_surface`/`xdg_toplevel`/EGL 与窗口 worker；失败不静默回退 X11。真实 renderer 的像素、resize、关闭重开已由 run #116 验收。当前加入 seat/pointer 的 motion、button、scroll 与移除时释放按键；`native_wayland_pointer_changes_rendered_pixels` 通过嵌套 Weston 的真实输入改变 shader 像素，已在 run #119 / `7e78eb3` hosted 验证；下载日志确认 enter 坐标、左键 press/release 到达 ViewState，并输出像素变化成功标记。**run #120 / aa9d25e 三平台 hosted 验收通过**：wl_keyboard 的 compositor keymap、修饰键/布局组、compose 组合字符、repeat、Enter/Leave 焦点与移除时释放按键，鼠标事件携带同 seat 修饰键。下载的 Linux 完整日志确认 Shift+A、组合 é、长按重复与释放停止，并通过像素变化断言（WAYLAND KEYBOARD VERIFIED）。compose 不等于 text-input-v3 输入法协议；后者尚未接入。**光标已由 [8dee3e8 三平台 hosted](https://github.com/aizcutei/sunmao/actions/runs/34226222440) 验收，三份产物下载及 SHA-256/ZIP CRC 校验通过**：set_mouse_cursor 通过 wl_pointer/wl_shm 设置主题图像、动画或隐藏；每次 enter 使用最新 serial，离开/移除停止更新。主题缺少某形状时记录诊断并使用默认箭头。native_wayland_cursor_changes_compositor_pixels_and_survives_reentry 从嵌套 Weston 输出像素验证十字/手形/隐藏/重入。Linux 日志确认双窗口切换时 leave(38)，返回原窗口 enter(44)，光标请求使用新 serial 44，随后 WAYLAND CURSOR VERIFIED 与测试 ok。外层 X11 窗口离开在 Weston 13 的 clear_pointer_focus 空实现下不产生 Wayland leave，故验收使用真实 Wayland 表面切换。**主动 focus 已由 [7ed7be0 三平台 hosted](https://github.com/aizcutei/sunmao/actions/runs/34231341466) 验收，三份产物已下载且 SHA-256/ZIP CRC 校验通过**：Window::focus 异步请求 xdg_activation_v1，附最近输入/focus serial 与 seat；Done 后发送 activate，不合成 Focused。焦点仍由 wl_keyboard Enter/Leave 决定；合成器可拒绝抢焦点，未提供扩展时记录诊断。令牌等待限 5 秒，同一窗口最多一项在途；不读取或修改宿主进程的 XDG_ACTIVATION_TOKEN。新增 native_wayland_focus_request_obeys_compositor_policy 与 removed_seats_never_supply_activation_serials，Sway 实测日志已核实：两次 token/activate 往返，后台请求未伪造 Focused，按键仍到 peer，关闭 peer 后原窗口收到真实 keyboard.enter(33)，随后 WAYLAND FOCUS VERIFIED。Weston 13 未公布该扩展，Sway 专项补足此验收。**output scaling 已由 [aa8694e 三平台 hosted](https://github.com/aizcutei/sunmao/actions/runs/34242729268) 验收，三份产物已下载且 SHA-256/ZIP CRC 校验通过**：SystemScaleFactor 优先使用 fractional-scale-v1（需 viewporter）或 wl_surface preferred_buffer_scale，旧 compositor 使用当前相交输出的最大 wl_output scale，scale/done 原子应用，移除输出清理其比例。surface 的 preferred scale 独立于输出集合，直到新通知到达前仍有效；Sway 1.9 禁用输出后保留最后的 2x preference，不能据剩余输出 1x 擅自覆盖。窗口 configure/min/max 与鼠标位置使用 surface-local 逻辑坐标，EGL 缓冲区使用按比例四舍五入后的物理像素，viewport/buffer scale 与下一帧一起提交；分数显式覆盖在缺少 viewporter 时如实拒绝。光标将 XCURSOR_SIZE 作为逻辑尺寸（缺省或无效时为 24），乘以向上取整的输出比例后显式加载 XCURSOR_THEME，避免主题库用环境变量覆盖物理尺寸；viewport 或整数 buffer scale 保持逻辑尺寸及热点；旧 compositor 若主题图像不能被比例整除则诊断并使用未缩放图像。新增 native_wayland_scaling_tracks_density_outputs_and_buffer_pixels，覆盖动态 1/2/1.5 比例、跨输出移动/移除、resize 和显式覆盖，读取实际 EGL 尺寸/边缘像素；新增 2x 光标 compositor 像素验证。**facade feature 传递已由 [910f522 三平台 hosted](https://github.com/aizcutei/sunmao/actions/runs/34248163394) 验收，三份产物 SHA-256/ZIP CRC 校验通过**：仅依赖 sunmao 的 facade_wayland_editor_renders_resizes_and_reopens 禁用 DISPLAY，经公开 prelude 构造浮动编辑器，读取实际红/绿 shader 像素并验证 resize、关闭和重开；CI 对 gui-wayland 独立编译及 doc-test，并要求 WAYLAND FACADE VERIFIED；完整 Linux 日志确认该标记实际输出，facade 测试 1 passed / 0 failed / 0 ignored。原生 Wayland 目前限 GL 浮动编辑器，WGPU/WebView 未接入；不能据此宣称所有后端均支持 Wayland。测试：`embedded_wayland_is_refused_while_x11_is_accepted`、`native_wayland_editor_renders_resizes_and_reopens`、`native_wayland_pointer_changes_rendered_pixels`、`removal_never_leaves_a_button_held` |
| accessibility（屏幕阅读器） | `accessibility_tree(&mut Stack, label) -> AccessibleNode`（role/label/可朗读值/归一化值/bounds/focus/disabled），role 由 `ParameterWidget::accessible_role()` **声明**；`accesskit_update(&AccessibleNode) -> accesskit::TreeUpdate`（Phase 4 M4/M5 落地，**off-by-default 的 `accessibility` feature**） | 无——VST3 不涉及 | 无——CLAP 不涉及 | **两格式无差异，因为 accessibility 根本不走插件格式**：VST3 与 CLAP 都没有 accessibility 通道，屏幕阅读器走的是操作系统 API（Windows UIA、macOS NSAccessibility、Linux AT-SPI），插件只能通过自己的编辑器窗口句柄参与。因此本行记录框架侧的两层：**(1)** 三家 OS API 共同需要的控件描述树；**(2)** 到 AccessKit 的翻译——AccessKit 维护着那三个原生适配器，SunMao 只写数据映射，没有平台代码。放在 off-by-default feature 后面的理由与 `text`/`clipboard` 相同：AccessKit 是真实依赖面（Linux 侧要 D-Bus），而描述树本身不依赖它。设计要点：role 必须声明而非从显示文本推断（选项恰好是 `"1"`/`"2"` 的下拉仍是下拉）；归一化值必须连同 min/max 一起给，否则屏幕阅读器算不出百分比；**非有限值不写**，否则会被念成乱码。**第三层已落地**：`TreeUpdate` 交给 `accesskit_windows`/`accesskit_macos`/`accesskit_unix` 并接进 baseview 的窗口生命周期——Windows 走 `WM_GETOBJECT` + 懒创建适配器（`Adapter::new` 不得在处理该消息期间调用），macOS 走 `SubclassingAdapter` 且必须在 AppKit 提问前装好（它靠 swizzle 工作，不能懒创建），Linux 走 `accesskit_unix` 的 AT-SPI 适配器、树每帧推入共享槽由 `Send` 的 activation handler 读回。**三平台共同降级：`ActionHandler` 不执行任何 action**——屏幕阅读器能读、不能改；AccessKit 的 trait 要求不支持的 action 必须无动作，故这是如实降级而非静默丢事件，`docs/phase3/compatibility.md` §2bis.4 同步记录。证据分层如实标注：**Windows 有真实往返断言**（`baseview` 的 UIA 测试用屏幕阅读器所用的同一套 API 读回 Slider/ComboBox/CheckBox，日志 `UIA VERIFIED`，run #100）；**macOS/Linux 目前只有编译级证据**（AXUIElement 在 CI 上受 TCC 限制，AT-SPI 需要 runner 未运行的总线）。`AccessibleRole` 标注 `#[non_exhaustive]`，新增 role 对桥接层兼容（见 compatibility.md §2bis.3，由一对 doc-test 机械守卫）。测试：`sunmao_gui::accessibility::tests`（6 个，含 `a_dropdown_of_numeric_options_is_still_a_dropdown`）、`sunmao_gui::accessibility_accesskit::tests`（7 个）、proptest `accessibility_property.rs`（4 条）与 `accesskit_property.rs`（4 条，钉住 AccessKit 要求的树形约束）、fixture `the_editor_describes_itself_to_assistive_technology`；CI 步骤 "Test the accessibility feature"（blocking，三平台） |
| preset 文件 | 宿主侧 `sunmao_unittest_runner host` 的 `preset save|load`（Phase 5 M1 落地；容器在 `tools/sunmao_unittest_runner/src/preset.rs`） | **有规定容器**：`.vstpreset` = `'VST3'` + `int32` 版本 + 32 字符 ASCII class ID + `int64` chunk list 偏移，随后 chunk 数据，最后 `'List'` + `int32` 条目数 + 每条 `{id(4), int64 offset, int64 size}`。整型一律小端（上游只在 `BYTEORDER == kBigEndian` 时交换）。SunMao 只写 `'Comp'`，读时保留他厂的 `'Cont'` | **没有宿主可写的容器**。`clap.preset-discovery` 把 preset 文件的格式交给插件自己声明（`CLAP_PRESET_DISCOVERY_LOCATION_FILE`），所以宿主侧"存 preset"只能是 `clap.state` 流的逐字节副本 | **两格式能力都落地，文件格式不同，这是规范决定的而非取舍**。差异的实际后果：VST3 preset 自带 class ID，宿主能在把任何字节交给插件之前拒绝别的插件的 preset；CLAP preset 没有身份头，只能靠插件自己的 state 魔数（`SMCLPRM\0`/`SMV3PRM\0`）挡下来。宿主对 CLAP 只做一件事——认出 `'VST3'` 开头的文件并如实说"这是 VST3 preset"，而不是把它塞给 `clap.state` 换一句无解释的 load failed。**严禁伪造**：没有为 CLAP 发明 SunMao 私有容器，那会产出别的 CLAP 宿主都读不了的文件。测试：`preset::tests` 共 13 条（round-trip、他厂 `'Cont'` 保留、截断/越界/无 `'Comp'`/未来版本/非 preset 逐条拒绝、CLAP 逐字节相等与跨格式拒绝）；CI 步骤 "Drive the interactive host over VST3 + CLAP"（blocking，三平台）另有 10 个必须失败的反向用例 |
| VST3 class ID 的字符串形式 | `preset::fuid_to_string(&[i8;16], com_compatible)`（Phase 5 M1 落地） | `FUID::toString`（`funknown.cpp`）有两条分支：`COM_COMPATIBLE` 下把前 8 字节当 `GuidStruct{uint32 Data1; uint16 Data2; uint16 Data3}` 按 `"%08X%04X%04X%s"` 打印，否则 16 字节顺序输出十六进制。`fplatform.h` 只在 `defined (_WIN32)` 下置 `COM_COMPATIBLE 1`，Linux 与 `__APPLE__` 均为 `0` | 不适用——CLAP 用字符串 id（`clap_plugin_descriptor.id`），没有这层 | **一处跨平台隐患，如实记录而不是掩盖**：上游 `INLINE_UID` 在两种布局下存**不同的字节**，正是为了让 `toString` 在所有平台得到**同一个字符串**——`vst3_sys::base::types::make_tuid` 已经照此实现（按 `cfg!(target_os = "windows")` 分支，并有 golden 字节测试）。但**插件自己的 class ID 走的不是这条路**：`vst3_rs::class_id_from_str` 产出与平台无关的固定 FNV 字节，于是同一个 SunMao 插件在 Windows 与 macOS/Linux 上会得到**两个不同的 class 字符串**，而 `.vstpreset` 存的正是这个字符串。后果：macOS 写出的 preset 的 class 串与 Windows 宿主算出的不一致。**本轮不改 class ID 的生成方式**——那会改变所有既有插件的身份、使已发布的工程与 preset 失效，属于独立立项。当前宿主的行为是自洽的：读写都用**本平台**的 `toString`，因此同平台往返正确，跨平台不匹配会被 class 串比较**明确拒绝**而不是悄悄载入错的 state。测试：`preset::tests::both_uid_layouts_print_the_same_canonical_string`（对着 `vst3_sys::make_tuid_for_layout` 双向核对）、`the_same_bytes_yield_different_class_strings_on_windows_and_elsewhere`（把这条隐患本身钉成断言） |
| 参数写入未知 ID | 宿主侧 `HostPlugin::param_set` | `IEditController::setParamNormalized` 返回非 `kResultOk`，宿主如实报错 | `clap_plugin_params.flush` 返回 **`void`**：携带未知 ID 的 param 事件被**静默丢弃**，规范把"只发 `params.get_info` 给过的 ID"的责任放在宿主 | **原先两格式不一致，本轮已抹平**：runner 的 CLAP 宿主此前对未知 ID 报告成功，而那次写入哪儿也没到。现在先用插件自己的 `params.get_value` 问一次，未知则如实报错。这不是给 CLAP 加语义，是宿主履行 CLAP 交给它的责任——一个对不存在的参数报成功的测试宿主，看不见插件把参数弄丢。测试：CI 步骤 "Drive the interactive host over VST3 + CLAP" 的 `unknown-param-vst3`/`unknown-param-clap` 两个反向用例（两格式都必须非零退出） |
| 参数回读精度 | 宿主侧 `HostPlugin::param_get` | `IEditController::getParamNormalized` 读的是 `vst3_rs::ParameterBridge` 的 **`AtomicU64`（f64）**副本，写多少读回多少 | `clap.params` 的 `get_value` 读的是插件真正的参数存储，即 `sunmao_core` 的 **`AtomicU32`（f32）** | **同一次写入经两格式读回的值不同**：写 0.9 再读回，VST3 差 0，CLAP 差 2.38e-8（正是 0.9 处的 f32 精度）。两者都不算错——VST3 宿主本就把 edit controller 当显示值的权威，而 CLAP 报的是 DSP 真正在用的数。**但它意味着 VST3 侧的回读并不能证明插件内部的值**，这一点此前没有写下来。直接后果：宿主的 `expect` **显式定义容差**（`DEFAULT_EXPECT_TOLERANCE = 1e-6`，位于 f32 误差之上）而不是比相等，脚本可另行指定，包括显式写 `0` 表示确实要逐位相同。统一两者的精度属独立立项，本轮不动已三平台验收的 VST3 路径。测试：`interactive::tests::expect_defaults_to_a_named_tolerance_and_accepts_an_explicit_one`、`a_zero_tolerance_is_allowed_because_it_is_a_deliberate_choice` |

## 记录规则

- 每个 milestone 落地时把对应行的"设计中"替换为最终 API 名，并补充实测差异。
- 单元/宿主测试必须覆盖表中每条降级行为；测试名写入对应行。
