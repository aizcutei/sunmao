[package]
name = "sunmao_syn_grouped_params"
version = "0.1.0"
edition = "2021"
description = "Grouped-parameter synth — Phase 3 acceptance fixture (param groups, smoothing, templates)"
license = "MIT OR Apache-2.0"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
sunmao = { path = "../../sunmao" }
