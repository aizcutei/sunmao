[package]
name = "sunmao_fx_meter"
version = "0.1.0"
edition = "2021"
description = "Peak/RMS meter — Phase 3 acceptance fixture (lock-free metering publish)"
license = "MIT OR Apache-2.0"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
sunmao = { path = "../../sunmao" }
sunmao_dsp = { path = "../../sunmao/dsp" }
