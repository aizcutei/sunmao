[package]
name = "sunmao_template_effect"
version = "0.1.0"
edition = "2021"
description = "Starting point for a new SunMao effect — copy, rename, edit"
license = "MIT OR Apache-2.0"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
sunmao = { path = "../../sunmao" }
