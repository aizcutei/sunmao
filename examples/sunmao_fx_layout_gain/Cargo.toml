[package]
name = "sunmao_fx_layout_gain"
version = "0.1.0"
edition = "2021"
description = "Mono/stereo layout negotiation — Phase 3 M1 acceptance fixture"
license = "MIT OR Apache-2.0"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
sunmao = { path = "../../sunmao" }
